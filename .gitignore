# Загальні файли та директорії, які не повинні бути в Git

# Змінні середовища - містять конфіденційні дані (токен бота)
.env

# Згенеровані файли даних
results.xlsx
*.png # Всі PNG файли, оскільки `layered_semi_circular_progress.png` генерується
# Якщо у вас є інші PNG, які повинні бути в репозиторії (наприклад, для візуальних ресурсів),
# то краще явно ігнорувати лише згенеровані, наприклад:
# layered_semi_circular_progress.png

# Файли з даними, які можуть змінюватися і не є частиною коду
# Враховуючи, що це дані "до" і "після", їх зазвичай не комітять.
# Якщо ви все ж хочете їх версіонувати, видаліть ці рядки.
#pass4.xlsx
#required.xlsx
#start_kvk.xlsx

# Папки для віртуальних середовищ Python
venv/
.venv/
env/
/bin/
/include/
/lib/
/share/

# Файли кешу Python
__pycache__/
*.pyc

# Директорії та файли IDE (наприклад, PyCharm, IntelliJ IDEA)
.idea/

# Файли операційної системи
.DS_Store # macOS
Thumbs.db # Windows